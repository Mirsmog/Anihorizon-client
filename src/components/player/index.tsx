import React from 'react'

import { TitleT } from '@/types'

interface PlayerProps extends TitleT {}

const Player: React.FC<PlayerProps> = ({ posters }) => {
	return <div></div>
}
export default Player
